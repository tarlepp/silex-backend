#
# /resources/config/dev/config.yml
#
# This file will import all the development (dev) environment settings for application, see each of files that are
# listed below for detailed information. Also note that this will import 'common' settings for the base of all
# application configurations.
#
imports:
  - { resource: ../common/config.yml }
  - { resource: database.yml }
  - { resource: parameters.yml }
  - { resource: security.yml }
  - { resource: @local.yml } # Note that this file is for you to override any of this level configurations
