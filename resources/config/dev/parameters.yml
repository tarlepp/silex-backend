#
# /resources/config/dev/parameters.yml
#
# This file contains parameter settings for development environment application.
#
# Note that these settings are common to all devs. If you want to override these settings in your _own_ environment
# you can create 'local.yml' file and override what ever you want in this environment.
#
debug: true
